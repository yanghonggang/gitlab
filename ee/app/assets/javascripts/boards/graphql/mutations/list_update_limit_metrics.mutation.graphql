#import "ee_else_ce/boards/graphql/fragments/board_list.fragment.graphql"

mutation boardListUpdateLimitMetrics($input: BoardListUpdateLimitMetricsInput!) {
  boardListUpdateLimitMetrics(input: $input) {
    list {
      ...BoardListFragment
    }
    errors
  }
}
